# Gemini Adapter

Integrates Sequoia with Google Gemini CLI's configuration directory layout.
Uses `StrategyFileReplace` — generates the Gemini prompt file as a full file with backup of any existing content.
Copies skills and commands to the appropriate Gemini CLI configuration paths.
