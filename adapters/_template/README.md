# Adapter Template

Copy-paste boilerplate for adding a new tool integration.
Implement the `ToolAdapter` interface from `adapters/interface.go`.
Replace all TODO comments with tool-specific logic.
See `CONTRIBUTING.md` for the step-by-step adapter development guide.
