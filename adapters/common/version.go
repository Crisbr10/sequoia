package common

// Version is the canonical Sequoia version string shared across all adapters
// and the CLI. It is embedded in installed skill files, system prompts,
// and the .sequoia-version marker file.
const Version = "1.0.5"
