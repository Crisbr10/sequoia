# Claude Code Adapter

Integrates Sequoia with Claude Code's `~/.claude/` directory layout.
Uses `StrategyMarkdownSections` — injects a delimited Sequoia section into `CLAUDE.md` using start/end markers.
Copies skills to `~/.claude/skills/sequoia/` and commands to `~/.claude/commands/`.
