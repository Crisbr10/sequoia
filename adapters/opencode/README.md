# OpenCode Adapter

Integrates Sequoia with OpenCode's `~/.config/opencode/` directory layout.
Uses `StrategyFileReplace` — generates `AGENTS.md` as a full file with backup of any existing content.
Copies skills to `~/.config/opencode/skills/sequoia/` and commands to `~/.config/opencode/commands/`.
