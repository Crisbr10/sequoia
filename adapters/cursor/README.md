# Cursor Adapter

Integrates Sequoia with Cursor's directory layout.
Uses `StrategyMarkdownSections` — injects a delimited Sequoia section into the Cursor rules file using start/end markers.
Copies skills and commands to the appropriate Cursor configuration paths.
