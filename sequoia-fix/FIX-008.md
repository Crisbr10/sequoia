# FIX-008 Summary — Movido a async

**Date**: 2026-05-12
**Files**: `internal/app/model.go`, `internal/app/update.go`, `internal/app/model_test.go`, `internal/app/model_internal_test.go`
**Tests**: 92/92 passing
**Build**: ✅

## Cambio

Mover `exec.LookPath("engram")` desde construcción síncrona del Model a un comando Bubbletea asíncrono (`detectEngram`) para que el TUI renderice inmediatamente sin bloquearse.

## Archivos modificados

| Archivo | Cambio |
|---------|--------|
| `model.go` | +`EngramDetectedMsg`, +`detectEngram()`, -`exec.LookPath` en `NewModel()`, `EngramAvailable: false`, `Init()` → `tea.Batch(detectEngram)` |
| `update.go` | +case `EngramDetectedMsg` en `Update()` |
| `model_test.go` | Test actualizado: `EngramAvailableDefaultsFalse`; +`NoExecLookPath` |
| `model_internal_test.go` | +`TestDetectEngram_ReturnsMsg`, +`TestEngramDetectedMsg_UpdatesModel` |

## Verificación

```powershell
go test ./internal/app/... -v -run "Engram|Detect"   # 3/3 PASS
go test ./internal/app/... -v                         # 92/92 PASS
go build -o sequoia.exe ./cmd/sequoia                 # SUCCESS
```
