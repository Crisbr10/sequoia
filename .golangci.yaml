version: "2"

linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell
    - revive
    - gosec
  settings:
    revive:
      rules:
        - name: exported
          arguments:
            - "checkPrivateReceivers"
            - "sayRepetitiveInsteadOfStutters"
  exclusions:
    rules:
      # Test files: allow relaxed error handling
      - path: _test\.go
        linters:
          - errcheck
      # main.go: intentional unchecked errors
      - path: cmd/sequoia/main\.go
        linters:
          - errcheck
      # adapter/ files: gosec blanket exclusion removed — use line-level nolints instead
      # (P4-007: narrowed gosec exclusions to line-level nolints with justification comments)
      # Mock adapter: no comments needed for generated mocks
      - path: adapters/testutil/mock_adapter\.go
        linters:
          - revive
      # Plugin loader: os.ReadDir with variable is by design
      - path: plugin/loader\.go
        linters:
          - gosec
      # Plugin implementation: no comments needed for interface methods
      - path: plugin/loader\.go
        linters:
          - revive
      # Embedded FS vars: don't need comments
      - path: adapters/common/embed\.go
        linters:
          - revive
      - path: adapters/common/installembed/embed\.go
        linters:
          - revive
      # Known unused vars in specific files
      - path: internal/tui/screens/uninstall\.go
        linters:
          - unused
      - path: adapters/opencode/paths\.go
        linters:
          - unused
      - path: adapters/claude/paths\.go
        linters:
          - unused
      # Pipeline runner: intentional unchecked errors
      - path: internal/pipeline/runner\.go
        linters:
          - errcheck
      # CodeGraph: exec.CommandContext with variable args is intentional (testability injection)
      - path: internal/codegraph/install\.go
        linters:
          - gosec
      - path: internal/codegraph/install_test\.go
        linters:
          - gosec

formatters:
  enable:
    - gofmt
    - goimports

issues:
  max-same-issues: 5
